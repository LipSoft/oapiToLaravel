@extends('master')

@section('content')

	<form action="{{=<% %>=}}{<%={{ }}=%>{{=<% %>=}}{<%={{ }}=%> URL::action('{{{capitalizedName}}}Controller@store') {{=<% %>=}}}<%={{ }}=%>{{=<% %>=}}}<%={{ }}=%>" method="POST">
		
		{!! csrf_field() !!}
		{{#fields}}
		<div class="showDiv">
			<div class="fieldDiv">{{{field}}}:</div>
			<div class="inputDiv">
				<input type="text" name="{{{field}}}" value="{{=<% %>=}}{<%={{ }}=%>{{=<% %>=}}{<%={{ }}=%>old('{{{field}}}') ? old('{{{field}}}') : ''{{=<% %>=}}}<%={{ }}=%>{{=<% %>=}}}<%={{ }}=%>" size="50" maxlength="50">
			</div>
		</div>
        {{/fields}}
		{{#belongsTo}}  
		<div class="showDiv">
			<div class="fieldDiv">{{{tableName}}}:</div>
			<div class="inputDiv">
				<select name="{{{columnName}}}">
					<option value=""></option>
					@foreach(${{{referencedTableName}}} as $value)
						<option {{=<% %>=}}{<%={{ }}=%>{{=<% %>=}}{<%={{ }}=%> (${{{tableName}}}->{{{columnName}}} == $value->id) ? 'selected' : '' {{=<% %>=}}}<%={{ }}=%>{{=<% %>=}}}<%={{ }}=%> value="{{$value->id}}">{{$value->name}}</option>
					@endforeach
				</select>
			</div>
		</div>  
		{{/belongsTo}}
		
		<div class="buttonDiv"><input class="submitInput" type="submit" value="Guardar"></div>

	</form>
		
	<div class="clearDiv"></div>
	<a class="buttonLink" href="{{=<% %>=}}{<%={{ }}=%>{{=<% %>=}}{<%={{ }}=%> URL::action('{{{capitalizedName}}}Controller@index') {{=<% %>=}}}<%={{ }}=%>{{=<% %>=}}}<%={{ }}=%>">
		<div class="buttonDiv">Voltar</div>
	</a>	

@endsection