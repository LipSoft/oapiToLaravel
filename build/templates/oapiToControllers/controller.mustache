<?php

namespace App\Http\Controllers;

use Illuminate\Http\Request;
use App\{{{name}}};

class {{{name}}}Controller extends Controller
{

    public function index()
    {      
        return {{{name}}}::all();  
    }

    public function create()
    {
        return view('{{{name}}}.create', [ ]);
    }

    public function show($id)
    {
        return {{{name}}}::find($id);
    }

    public function edit($id)
    {
        return view('{{{name}}}.edit', ['{{{name}}}' => {{{name}}}::find($id) ]);
    }

    public function store(Request $request)
    {
        $input = $request->all();        
        ${{{name}}} = new {{{name}}};
        //${{{name}}}->update($request->all());
        {{#fields}}
        if (isset($input['{{{field}}}'])) ${{{name}}}->{{{field}}} = $input['{{{field}}}'];                        
        {{/fields}}
        
        ${{{name}}}->save();
        return ${{{name}}};
    }
    
    public function update(Request $request, $id)
    {
        $input = $request->all();
        //update with all fields and do array ith isset
        ${{{name}}} = {{{name}}}::find($id);
        //${{{name}}}->update($request->all());
        {{#fields}}
        ${{{name}}}->{{{field}}} = $input['{{{field}}}'];        
        {{/fields}}
        ${{{name}}}->save();
        return ${{{name}}};
    }

    public function destroy($id)
    {
        return {{{name}}}::destroy($id);
    }
}
